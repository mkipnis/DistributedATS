// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file ExecutionReportTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "ExecutionReport.h"
#include "ExecutionReportTypeObject.h"
#include <mutex>
#include <utility>
#include <sstream>
#include <fastdds/rtps/common/CdrSerialization.hpp>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>

using namespace eprosima::fastrtps::rtps;

void registerExecutionReportTypes()
{
    static std::once_flag once_flag;
    std::call_once(once_flag, []()
            {
                TypeObjectFactory *factory = TypeObjectFactory::get_instance();
                factory->add_type_object("DistributedATS::Header", DistributedATS::GetHeaderIdentifier(true),
                        DistributedATS::GetHeaderObject(true));
                factory->add_type_object("DistributedATS::Header", DistributedATS::GetHeaderIdentifier(false),
                        DistributedATS::GetHeaderObject(false));



                factory->add_type_object("DistributedATS_ExecutionReport::ExecutionReport", DistributedATS_ExecutionReport::GetExecutionReportIdentifier(true),
                        DistributedATS_ExecutionReport::GetExecutionReportObject(true));
                factory->add_type_object("DistributedATS_ExecutionReport::ExecutionReport", DistributedATS_ExecutionReport::GetExecutionReportIdentifier(false),
                        DistributedATS_ExecutionReport::GetExecutionReportObject(false));


            });
}

namespace DistributedATS_ExecutionReport {


    const TypeIdentifier* GetExecutionReportIdentifier(bool complete)
    {
        const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("ExecutionReport", complete);
        if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
        {
            return c_identifier;
        }

        GetExecutionReportObject(complete); // Generated inside
        return TypeObjectFactory::get_instance()->get_type_identifier("ExecutionReport", complete);
    }

    const TypeObject* GetExecutionReportObject(bool complete)
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ExecutionReport", complete);
        if (c_type_object != nullptr)
        {
            return c_type_object;
        }
        else if (complete)
        {
            return GetCompleteExecutionReportObject();
        }
        //else
        return GetMinimalExecutionReportObject();
    }

    const TypeObject* GetMinimalExecutionReportObject()
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ExecutionReport", false);
        if (c_type_object != nullptr)
        {
            return c_type_object;
        }

        TypeObject *type_object = new TypeObject();
        type_object->_d(EK_MINIMAL);
        type_object->minimal()._d(TK_STRUCTURE);

        type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
        type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
        type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
        type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
        type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

        MemberId memberId = 0;
        MinimalStructMember mst_header;
        mst_header.common().member_id(memberId++);
        mst_header.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_header.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_header.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_header.common().member_flags().IS_OPTIONAL(false);
        mst_header.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_header.common().member_flags().IS_KEY(false);
        mst_header.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_header.common().member_type_id(*DistributedATS::GetHeaderIdentifier(false));

        MD5 header_hash("header");
        for(int i = 0; i < 4; ++i)
        {
            mst_header.detail().name_hash()[i] = header_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_header);

        MinimalStructMember mst_OrderID;
        mst_OrderID.common().member_id(memberId++);
        mst_OrderID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrderID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrderID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrderID.common().member_flags().IS_OPTIONAL(false);
        mst_OrderID.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrderID.common().member_flags().IS_KEY(false);
        mst_OrderID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrderID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 OrderID_hash("OrderID");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrderID.detail().name_hash()[i] = OrderID_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrderID);

        MinimalStructMember mst_OrigClOrdID;
        mst_OrigClOrdID.common().member_id(memberId++);
        mst_OrigClOrdID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrigClOrdID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrigClOrdID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrigClOrdID.common().member_flags().IS_OPTIONAL(false);
        mst_OrigClOrdID.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrigClOrdID.common().member_flags().IS_KEY(false);
        mst_OrigClOrdID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrigClOrdID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 OrigClOrdID_hash("OrigClOrdID");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrigClOrdID.detail().name_hash()[i] = OrigClOrdID_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrigClOrdID);

        MinimalStructMember mst_ExecID;
        mst_ExecID.common().member_id(memberId++);
        mst_ExecID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_ExecID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_ExecID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_ExecID.common().member_flags().IS_OPTIONAL(false);
        mst_ExecID.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_ExecID.common().member_flags().IS_KEY(false);
        mst_ExecID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_ExecID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 ExecID_hash("ExecID");
        for(int i = 0; i < 4; ++i)
        {
            mst_ExecID.detail().name_hash()[i] = ExecID_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_ExecID);

        MinimalStructMember mst_ExecType;
        mst_ExecType.common().member_id(memberId++);
        mst_ExecType.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_ExecType.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_ExecType.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_ExecType.common().member_flags().IS_OPTIONAL(false);
        mst_ExecType.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_ExecType.common().member_flags().IS_KEY(false);
        mst_ExecType.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_ExecType.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        MD5 ExecType_hash("ExecType");
        for(int i = 0; i < 4; ++i)
        {
            mst_ExecType.detail().name_hash()[i] = ExecType_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_ExecType);

        MinimalStructMember mst_OrdStatus;
        mst_OrdStatus.common().member_id(memberId++);
        mst_OrdStatus.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrdStatus.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrdStatus.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrdStatus.common().member_flags().IS_OPTIONAL(false);
        mst_OrdStatus.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrdStatus.common().member_flags().IS_KEY(false);
        mst_OrdStatus.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrdStatus.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        MD5 OrdStatus_hash("OrdStatus");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrdStatus.detail().name_hash()[i] = OrdStatus_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrdStatus);

        MinimalStructMember mst_OrdRejReason;
        mst_OrdRejReason.common().member_id(memberId++);
        mst_OrdRejReason.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrdRejReason.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrdRejReason.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrdRejReason.common().member_flags().IS_OPTIONAL(false);
        mst_OrdRejReason.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrdRejReason.common().member_flags().IS_KEY(false);
        mst_OrdRejReason.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrdRejReason.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 OrdRejReason_hash("OrdRejReason");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrdRejReason.detail().name_hash()[i] = OrdRejReason_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrdRejReason);

        MinimalStructMember mst_Symbol;
        mst_Symbol.common().member_id(memberId++);
        mst_Symbol.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Symbol.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Symbol.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Symbol.common().member_flags().IS_OPTIONAL(false);
        mst_Symbol.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Symbol.common().member_flags().IS_KEY(false);
        mst_Symbol.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Symbol.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 Symbol_hash("Symbol");
        for(int i = 0; i < 4; ++i)
        {
            mst_Symbol.detail().name_hash()[i] = Symbol_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Symbol);

        MinimalStructMember mst_SecurityExchange;
        mst_SecurityExchange.common().member_id(memberId++);
        mst_SecurityExchange.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_SecurityExchange.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_SecurityExchange.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_SecurityExchange.common().member_flags().IS_OPTIONAL(false);
        mst_SecurityExchange.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_SecurityExchange.common().member_flags().IS_KEY(false);
        mst_SecurityExchange.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_SecurityExchange.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 SecurityExchange_hash("SecurityExchange");
        for(int i = 0; i < 4; ++i)
        {
            mst_SecurityExchange.detail().name_hash()[i] = SecurityExchange_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_SecurityExchange);

        MinimalStructMember mst_Side;
        mst_Side.common().member_id(memberId++);
        mst_Side.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Side.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Side.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Side.common().member_flags().IS_OPTIONAL(false);
        mst_Side.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Side.common().member_flags().IS_KEY(false);
        mst_Side.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Side.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        MD5 Side_hash("Side");
        for(int i = 0; i < 4; ++i)
        {
            mst_Side.detail().name_hash()[i] = Side_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Side);

        MinimalStructMember mst_OrderQty;
        mst_OrderQty.common().member_id(memberId++);
        mst_OrderQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrderQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrderQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrderQty.common().member_flags().IS_OPTIONAL(false);
        mst_OrderQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrderQty.common().member_flags().IS_KEY(false);
        mst_OrderQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrderQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 OrderQty_hash("OrderQty");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrderQty.detail().name_hash()[i] = OrderQty_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrderQty);

        MinimalStructMember mst_OrdType;
        mst_OrdType.common().member_id(memberId++);
        mst_OrdType.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_OrdType.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_OrdType.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_OrdType.common().member_flags().IS_OPTIONAL(false);
        mst_OrdType.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_OrdType.common().member_flags().IS_KEY(false);
        mst_OrdType.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_OrdType.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        MD5 OrdType_hash("OrdType");
        for(int i = 0; i < 4; ++i)
        {
            mst_OrdType.detail().name_hash()[i] = OrdType_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_OrdType);

        MinimalStructMember mst_Price;
        mst_Price.common().member_id(memberId++);
        mst_Price.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Price.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Price.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Price.common().member_flags().IS_OPTIONAL(false);
        mst_Price.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Price.common().member_flags().IS_KEY(false);
        mst_Price.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Price.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        MD5 Price_hash("Price");
        for(int i = 0; i < 4; ++i)
        {
            mst_Price.detail().name_hash()[i] = Price_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Price);

        MinimalStructMember mst_StopPx;
        mst_StopPx.common().member_id(memberId++);
        mst_StopPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_StopPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_StopPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_StopPx.common().member_flags().IS_OPTIONAL(false);
        mst_StopPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_StopPx.common().member_flags().IS_KEY(false);
        mst_StopPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_StopPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        MD5 StopPx_hash("StopPx");
        for(int i = 0; i < 4; ++i)
        {
            mst_StopPx.detail().name_hash()[i] = StopPx_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_StopPx);

        MinimalStructMember mst_TimeInForce;
        mst_TimeInForce.common().member_id(memberId++);
        mst_TimeInForce.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_TimeInForce.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_TimeInForce.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_TimeInForce.common().member_flags().IS_OPTIONAL(false);
        mst_TimeInForce.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_TimeInForce.common().member_flags().IS_KEY(false);
        mst_TimeInForce.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_TimeInForce.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        MD5 TimeInForce_hash("TimeInForce");
        for(int i = 0; i < 4; ++i)
        {
            mst_TimeInForce.detail().name_hash()[i] = TimeInForce_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_TimeInForce);

        MinimalStructMember mst_ExecInst;
        mst_ExecInst.common().member_id(memberId++);
        mst_ExecInst.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_ExecInst.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_ExecInst.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_ExecInst.common().member_flags().IS_OPTIONAL(false);
        mst_ExecInst.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_ExecInst.common().member_flags().IS_KEY(false);
        mst_ExecInst.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_ExecInst.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 ExecInst_hash("ExecInst");
        for(int i = 0; i < 4; ++i)
        {
            mst_ExecInst.detail().name_hash()[i] = ExecInst_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_ExecInst);

        MinimalStructMember mst_LastQty;
        mst_LastQty.common().member_id(memberId++);
        mst_LastQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_LastQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_LastQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_LastQty.common().member_flags().IS_OPTIONAL(false);
        mst_LastQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_LastQty.common().member_flags().IS_KEY(false);
        mst_LastQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_LastQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 LastQty_hash("LastQty");
        for(int i = 0; i < 4; ++i)
        {
            mst_LastQty.detail().name_hash()[i] = LastQty_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_LastQty);

        MinimalStructMember mst_LastPx;
        mst_LastPx.common().member_id(memberId++);
        mst_LastPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_LastPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_LastPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_LastPx.common().member_flags().IS_OPTIONAL(false);
        mst_LastPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_LastPx.common().member_flags().IS_KEY(false);
        mst_LastPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_LastPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        MD5 LastPx_hash("LastPx");
        for(int i = 0; i < 4; ++i)
        {
            mst_LastPx.detail().name_hash()[i] = LastPx_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_LastPx);

        MinimalStructMember mst_LeavesQty;
        mst_LeavesQty.common().member_id(memberId++);
        mst_LeavesQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_LeavesQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_LeavesQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_LeavesQty.common().member_flags().IS_OPTIONAL(false);
        mst_LeavesQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_LeavesQty.common().member_flags().IS_KEY(false);
        mst_LeavesQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_LeavesQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 LeavesQty_hash("LeavesQty");
        for(int i = 0; i < 4; ++i)
        {
            mst_LeavesQty.detail().name_hash()[i] = LeavesQty_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_LeavesQty);

        MinimalStructMember mst_CumQty;
        mst_CumQty.common().member_id(memberId++);
        mst_CumQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_CumQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_CumQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_CumQty.common().member_flags().IS_OPTIONAL(false);
        mst_CumQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_CumQty.common().member_flags().IS_KEY(false);
        mst_CumQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_CumQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 CumQty_hash("CumQty");
        for(int i = 0; i < 4; ++i)
        {
            mst_CumQty.detail().name_hash()[i] = CumQty_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_CumQty);

        MinimalStructMember mst_AvgPx;
        mst_AvgPx.common().member_id(memberId++);
        mst_AvgPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_AvgPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_AvgPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_AvgPx.common().member_flags().IS_OPTIONAL(false);
        mst_AvgPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_AvgPx.common().member_flags().IS_KEY(false);
        mst_AvgPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_AvgPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        MD5 AvgPx_hash("AvgPx");
        for(int i = 0; i < 4; ++i)
        {
            mst_AvgPx.detail().name_hash()[i] = AvgPx_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_AvgPx);

        MinimalStructMember mst_TransactTime;
        mst_TransactTime.common().member_id(memberId++);
        mst_TransactTime.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_TransactTime.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_TransactTime.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_TransactTime.common().member_flags().IS_OPTIONAL(false);
        mst_TransactTime.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_TransactTime.common().member_flags().IS_KEY(false);
        mst_TransactTime.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_TransactTime.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint64_t", false));


        MD5 TransactTime_hash("TransactTime");
        for(int i = 0; i < 4; ++i)
        {
            mst_TransactTime.detail().name_hash()[i] = TransactTime_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_TransactTime);

        MinimalStructMember mst_Text;
        mst_Text.common().member_id(memberId++);
        mst_Text.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Text.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Text.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Text.common().member_flags().IS_OPTIONAL(false);
        mst_Text.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Text.common().member_flags().IS_KEY(false);
        mst_Text.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Text.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 Text_hash("Text");
        for(int i = 0; i < 4; ++i)
        {
            mst_Text.detail().name_hash()[i] = Text_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Text);


        // Header
        // TODO Inheritance
        //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
        //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
        TypeIdentifier identifier;
        identifier._d(EK_MINIMAL);

        eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
        size_t current_alignment {0};
        SerializedPayload_t payload(static_cast<uint32_t>(
            calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
        eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
        // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
        eprosima::fastcdr::Cdr ser(
            fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
            eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
        payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        ser << *type_object;
    #if FASTCDR_VERSION_MAJOR == 1
        payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    #else
        payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
    #endif // FASTCDR_VERSION_MAJOR == 1
        MD5 objectHash;
        objectHash.update((char*)payload.data, payload.length);
        objectHash.finalize();
        for(int i = 0; i < 14; ++i)
        {
            identifier.equivalence_hash()[i] = objectHash.digest[i];
        }

        TypeObjectFactory::get_instance()->add_type_object("ExecutionReport", &identifier, type_object);
        delete type_object;
        return TypeObjectFactory::get_instance()->get_type_object("ExecutionReport", false);
    }

    const TypeObject* GetCompleteExecutionReportObject()
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ExecutionReport", true);
        if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
        {
            return c_type_object;
        }

        TypeObject *type_object = new TypeObject();
        type_object->_d(EK_COMPLETE);
        type_object->complete()._d(TK_STRUCTURE);

        type_object->complete().struct_type().struct_flags().IS_FINAL(false);
        type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
        type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
        type_object->complete().struct_type().struct_flags().IS_NESTED(false);
        type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

        MemberId memberId = 0;
        CompleteStructMember cst_header;
        cst_header.common().member_id(memberId++);
        cst_header.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_header.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_header.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_header.common().member_flags().IS_OPTIONAL(false);
        cst_header.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_header.common().member_flags().IS_KEY(false);
        cst_header.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_header.common().member_type_id(*DistributedATS::GetHeaderIdentifier(true));

        cst_header.detail().name("header");

        type_object->complete().struct_type().member_seq().emplace_back(cst_header);

        CompleteStructMember cst_OrderID;
        cst_OrderID.common().member_id(memberId++);
        cst_OrderID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrderID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrderID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrderID.common().member_flags().IS_OPTIONAL(false);
        cst_OrderID.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrderID.common().member_flags().IS_KEY(false);
        cst_OrderID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrderID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_OrderID.detail().name("OrderID");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrderID);

        CompleteStructMember cst_OrigClOrdID;
        cst_OrigClOrdID.common().member_id(memberId++);
        cst_OrigClOrdID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrigClOrdID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrigClOrdID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrigClOrdID.common().member_flags().IS_OPTIONAL(false);
        cst_OrigClOrdID.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrigClOrdID.common().member_flags().IS_KEY(false);
        cst_OrigClOrdID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrigClOrdID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_OrigClOrdID.detail().name("OrigClOrdID");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrigClOrdID);

        CompleteStructMember cst_ExecID;
        cst_ExecID.common().member_id(memberId++);
        cst_ExecID.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_ExecID.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_ExecID.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_ExecID.common().member_flags().IS_OPTIONAL(false);
        cst_ExecID.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_ExecID.common().member_flags().IS_KEY(false);
        cst_ExecID.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_ExecID.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_ExecID.detail().name("ExecID");

        type_object->complete().struct_type().member_seq().emplace_back(cst_ExecID);

        CompleteStructMember cst_ExecType;
        cst_ExecType.common().member_id(memberId++);
        cst_ExecType.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_ExecType.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_ExecType.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_ExecType.common().member_flags().IS_OPTIONAL(false);
        cst_ExecType.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_ExecType.common().member_flags().IS_KEY(false);
        cst_ExecType.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_ExecType.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        cst_ExecType.detail().name("ExecType");

        type_object->complete().struct_type().member_seq().emplace_back(cst_ExecType);

        CompleteStructMember cst_OrdStatus;
        cst_OrdStatus.common().member_id(memberId++);
        cst_OrdStatus.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrdStatus.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrdStatus.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrdStatus.common().member_flags().IS_OPTIONAL(false);
        cst_OrdStatus.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrdStatus.common().member_flags().IS_KEY(false);
        cst_OrdStatus.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrdStatus.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        cst_OrdStatus.detail().name("OrdStatus");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrdStatus);

        CompleteStructMember cst_OrdRejReason;
        cst_OrdRejReason.common().member_id(memberId++);
        cst_OrdRejReason.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrdRejReason.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrdRejReason.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrdRejReason.common().member_flags().IS_OPTIONAL(false);
        cst_OrdRejReason.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrdRejReason.common().member_flags().IS_KEY(false);
        cst_OrdRejReason.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrdRejReason.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_OrdRejReason.detail().name("OrdRejReason");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrdRejReason);

        CompleteStructMember cst_Symbol;
        cst_Symbol.common().member_id(memberId++);
        cst_Symbol.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Symbol.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Symbol.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Symbol.common().member_flags().IS_OPTIONAL(false);
        cst_Symbol.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Symbol.common().member_flags().IS_KEY(false);
        cst_Symbol.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Symbol.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_Symbol.detail().name("Symbol");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Symbol);

        CompleteStructMember cst_SecurityExchange;
        cst_SecurityExchange.common().member_id(memberId++);
        cst_SecurityExchange.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_SecurityExchange.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_SecurityExchange.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_SecurityExchange.common().member_flags().IS_OPTIONAL(false);
        cst_SecurityExchange.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_SecurityExchange.common().member_flags().IS_KEY(false);
        cst_SecurityExchange.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_SecurityExchange.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_SecurityExchange.detail().name("SecurityExchange");

        type_object->complete().struct_type().member_seq().emplace_back(cst_SecurityExchange);

        CompleteStructMember cst_Side;
        cst_Side.common().member_id(memberId++);
        cst_Side.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Side.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Side.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Side.common().member_flags().IS_OPTIONAL(false);
        cst_Side.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Side.common().member_flags().IS_KEY(false);
        cst_Side.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Side.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        cst_Side.detail().name("Side");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Side);

        CompleteStructMember cst_OrderQty;
        cst_OrderQty.common().member_id(memberId++);
        cst_OrderQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrderQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrderQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrderQty.common().member_flags().IS_OPTIONAL(false);
        cst_OrderQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrderQty.common().member_flags().IS_KEY(false);
        cst_OrderQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrderQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_OrderQty.detail().name("OrderQty");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrderQty);

        CompleteStructMember cst_OrdType;
        cst_OrdType.common().member_id(memberId++);
        cst_OrdType.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_OrdType.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_OrdType.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_OrdType.common().member_flags().IS_OPTIONAL(false);
        cst_OrdType.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_OrdType.common().member_flags().IS_KEY(false);
        cst_OrdType.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_OrdType.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        cst_OrdType.detail().name("OrdType");

        type_object->complete().struct_type().member_seq().emplace_back(cst_OrdType);

        CompleteStructMember cst_Price;
        cst_Price.common().member_id(memberId++);
        cst_Price.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Price.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Price.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Price.common().member_flags().IS_OPTIONAL(false);
        cst_Price.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Price.common().member_flags().IS_KEY(false);
        cst_Price.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Price.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        cst_Price.detail().name("Price");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Price);

        CompleteStructMember cst_StopPx;
        cst_StopPx.common().member_id(memberId++);
        cst_StopPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_StopPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_StopPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_StopPx.common().member_flags().IS_OPTIONAL(false);
        cst_StopPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_StopPx.common().member_flags().IS_KEY(false);
        cst_StopPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_StopPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        cst_StopPx.detail().name("StopPx");

        type_object->complete().struct_type().member_seq().emplace_back(cst_StopPx);

        CompleteStructMember cst_TimeInForce;
        cst_TimeInForce.common().member_id(memberId++);
        cst_TimeInForce.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_TimeInForce.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_TimeInForce.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_TimeInForce.common().member_flags().IS_OPTIONAL(false);
        cst_TimeInForce.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_TimeInForce.common().member_flags().IS_KEY(false);
        cst_TimeInForce.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_TimeInForce.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("char", false));


        cst_TimeInForce.detail().name("TimeInForce");

        type_object->complete().struct_type().member_seq().emplace_back(cst_TimeInForce);

        CompleteStructMember cst_ExecInst;
        cst_ExecInst.common().member_id(memberId++);
        cst_ExecInst.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_ExecInst.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_ExecInst.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_ExecInst.common().member_flags().IS_OPTIONAL(false);
        cst_ExecInst.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_ExecInst.common().member_flags().IS_KEY(false);
        cst_ExecInst.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_ExecInst.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_ExecInst.detail().name("ExecInst");

        type_object->complete().struct_type().member_seq().emplace_back(cst_ExecInst);

        CompleteStructMember cst_LastQty;
        cst_LastQty.common().member_id(memberId++);
        cst_LastQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_LastQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_LastQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_LastQty.common().member_flags().IS_OPTIONAL(false);
        cst_LastQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_LastQty.common().member_flags().IS_KEY(false);
        cst_LastQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_LastQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_LastQty.detail().name("LastQty");

        type_object->complete().struct_type().member_seq().emplace_back(cst_LastQty);

        CompleteStructMember cst_LastPx;
        cst_LastPx.common().member_id(memberId++);
        cst_LastPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_LastPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_LastPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_LastPx.common().member_flags().IS_OPTIONAL(false);
        cst_LastPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_LastPx.common().member_flags().IS_KEY(false);
        cst_LastPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_LastPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        cst_LastPx.detail().name("LastPx");

        type_object->complete().struct_type().member_seq().emplace_back(cst_LastPx);

        CompleteStructMember cst_LeavesQty;
        cst_LeavesQty.common().member_id(memberId++);
        cst_LeavesQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_LeavesQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_LeavesQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_LeavesQty.common().member_flags().IS_OPTIONAL(false);
        cst_LeavesQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_LeavesQty.common().member_flags().IS_KEY(false);
        cst_LeavesQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_LeavesQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_LeavesQty.detail().name("LeavesQty");

        type_object->complete().struct_type().member_seq().emplace_back(cst_LeavesQty);

        CompleteStructMember cst_CumQty;
        cst_CumQty.common().member_id(memberId++);
        cst_CumQty.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_CumQty.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_CumQty.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_CumQty.common().member_flags().IS_OPTIONAL(false);
        cst_CumQty.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_CumQty.common().member_flags().IS_KEY(false);
        cst_CumQty.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_CumQty.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_CumQty.detail().name("CumQty");

        type_object->complete().struct_type().member_seq().emplace_back(cst_CumQty);

        CompleteStructMember cst_AvgPx;
        cst_AvgPx.common().member_id(memberId++);
        cst_AvgPx.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_AvgPx.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_AvgPx.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_AvgPx.common().member_flags().IS_OPTIONAL(false);
        cst_AvgPx.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_AvgPx.common().member_flags().IS_KEY(false);
        cst_AvgPx.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_AvgPx.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("float", false));


        cst_AvgPx.detail().name("AvgPx");

        type_object->complete().struct_type().member_seq().emplace_back(cst_AvgPx);

        CompleteStructMember cst_TransactTime;
        cst_TransactTime.common().member_id(memberId++);
        cst_TransactTime.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_TransactTime.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_TransactTime.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_TransactTime.common().member_flags().IS_OPTIONAL(false);
        cst_TransactTime.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_TransactTime.common().member_flags().IS_KEY(false);
        cst_TransactTime.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_TransactTime.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint64_t", false));


        cst_TransactTime.detail().name("TransactTime");

        type_object->complete().struct_type().member_seq().emplace_back(cst_TransactTime);

        CompleteStructMember cst_Text;
        cst_Text.common().member_id(memberId++);
        cst_Text.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Text.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Text.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Text.common().member_flags().IS_OPTIONAL(false);
        cst_Text.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Text.common().member_flags().IS_KEY(false);
        cst_Text.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Text.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_Text.detail().name("Text");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Text);


        // Header
        type_object->complete().struct_type().header().detail().type_name("ExecutionReport");
        // TODO inheritance
        TypeIdentifier identifier;
        identifier._d(EK_COMPLETE);

        eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
        size_t current_alignment {0};
        SerializedPayload_t payload(static_cast<uint32_t>(
            calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
        eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
        // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
        eprosima::fastcdr::Cdr ser(
            fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
            eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
        payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        ser << *type_object;
    #if FASTCDR_VERSION_MAJOR == 1
        payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    #else
        payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
    #endif // FASTCDR_VERSION_MAJOR == 1
        MD5 objectHash;
        objectHash.update((char*)payload.data, payload.length);
        objectHash.finalize();
        for(int i = 0; i < 14; ++i)
        {
            identifier.equivalence_hash()[i] = objectHash.digest[i];
        }

        TypeObjectFactory::get_instance()->add_type_object("ExecutionReport", &identifier, type_object);
        delete type_object;
        return TypeObjectFactory::get_instance()->get_type_object("ExecutionReport", true);
    }

} // namespace DistributedATS_ExecutionReport