// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file LogonTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "Logon.h"
#include "LogonTypeObject.h"
#include <mutex>
#include <utility>
#include <sstream>
#include <fastdds/rtps/common/CdrSerialization.hpp>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>

using namespace eprosima::fastrtps::rtps;

void registerLogonTypes()
{
    static std::once_flag once_flag;
    std::call_once(once_flag, []()
            {
                TypeObjectFactory *factory = TypeObjectFactory::get_instance();
                factory->add_type_object("DistributedATS::Header", DistributedATS::GetHeaderIdentifier(true),
                        DistributedATS::GetHeaderObject(true));
                factory->add_type_object("DistributedATS::Header", DistributedATS::GetHeaderIdentifier(false),
                        DistributedATS::GetHeaderObject(false));



                factory->add_type_object("DistributedATS_Logon::Logon", DistributedATS_Logon::GetLogonIdentifier(true),
                        DistributedATS_Logon::GetLogonObject(true));
                factory->add_type_object("DistributedATS_Logon::Logon", DistributedATS_Logon::GetLogonIdentifier(false),
                        DistributedATS_Logon::GetLogonObject(false));


            });
}

namespace DistributedATS_Logon {


    const TypeIdentifier* GetLogonIdentifier(bool complete)
    {
        const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("Logon", complete);
        if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
        {
            return c_identifier;
        }

        GetLogonObject(complete); // Generated inside
        return TypeObjectFactory::get_instance()->get_type_identifier("Logon", complete);
    }

    const TypeObject* GetLogonObject(bool complete)
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("Logon", complete);
        if (c_type_object != nullptr)
        {
            return c_type_object;
        }
        else if (complete)
        {
            return GetCompleteLogonObject();
        }
        //else
        return GetMinimalLogonObject();
    }

    const TypeObject* GetMinimalLogonObject()
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("Logon", false);
        if (c_type_object != nullptr)
        {
            return c_type_object;
        }

        TypeObject *type_object = new TypeObject();
        type_object->_d(EK_MINIMAL);
        type_object->minimal()._d(TK_STRUCTURE);

        type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
        type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
        type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
        type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
        type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

        MemberId memberId = 0;
        MinimalStructMember mst_header;
        mst_header.common().member_id(memberId++);
        mst_header.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_header.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_header.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_header.common().member_flags().IS_OPTIONAL(false);
        mst_header.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_header.common().member_flags().IS_KEY(false);
        mst_header.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_header.common().member_type_id(*DistributedATS::GetHeaderIdentifier(false));

        MD5 header_hash("header");
        for(int i = 0; i < 4; ++i)
        {
            mst_header.detail().name_hash()[i] = header_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_header);

        MinimalStructMember mst_EncryptMethod;
        mst_EncryptMethod.common().member_id(memberId++);
        mst_EncryptMethod.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_EncryptMethod.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_EncryptMethod.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_EncryptMethod.common().member_flags().IS_OPTIONAL(false);
        mst_EncryptMethod.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_EncryptMethod.common().member_flags().IS_KEY(false);
        mst_EncryptMethod.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_EncryptMethod.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 EncryptMethod_hash("EncryptMethod");
        for(int i = 0; i < 4; ++i)
        {
            mst_EncryptMethod.detail().name_hash()[i] = EncryptMethod_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_EncryptMethod);

        MinimalStructMember mst_HeartBtInt;
        mst_HeartBtInt.common().member_id(memberId++);
        mst_HeartBtInt.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_HeartBtInt.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_HeartBtInt.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_HeartBtInt.common().member_flags().IS_OPTIONAL(false);
        mst_HeartBtInt.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_HeartBtInt.common().member_flags().IS_KEY(false);
        mst_HeartBtInt.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_HeartBtInt.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        MD5 HeartBtInt_hash("HeartBtInt");
        for(int i = 0; i < 4; ++i)
        {
            mst_HeartBtInt.detail().name_hash()[i] = HeartBtInt_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_HeartBtInt);

        MinimalStructMember mst_RawData;
        mst_RawData.common().member_id(memberId++);
        mst_RawData.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_RawData.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_RawData.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_RawData.common().member_flags().IS_OPTIONAL(false);
        mst_RawData.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_RawData.common().member_flags().IS_KEY(false);
        mst_RawData.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_RawData.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 RawData_hash("RawData");
        for(int i = 0; i < 4; ++i)
        {
            mst_RawData.detail().name_hash()[i] = RawData_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_RawData);

        MinimalStructMember mst_Username;
        mst_Username.common().member_id(memberId++);
        mst_Username.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Username.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Username.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Username.common().member_flags().IS_OPTIONAL(false);
        mst_Username.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Username.common().member_flags().IS_KEY(false);
        mst_Username.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Username.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 Username_hash("Username");
        for(int i = 0; i < 4; ++i)
        {
            mst_Username.detail().name_hash()[i] = Username_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Username);

        MinimalStructMember mst_Password;
        mst_Password.common().member_id(memberId++);
        mst_Password.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        mst_Password.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        mst_Password.common().member_flags().IS_EXTERNAL(false); // Unsupported
        mst_Password.common().member_flags().IS_OPTIONAL(false);
        mst_Password.common().member_flags().IS_MUST_UNDERSTAND(false);
        mst_Password.common().member_flags().IS_KEY(false);
        mst_Password.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        mst_Password.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        MD5 Password_hash("Password");
        for(int i = 0; i < 4; ++i)
        {
            mst_Password.detail().name_hash()[i] = Password_hash.digest[i];
        }
        type_object->minimal().struct_type().member_seq().emplace_back(mst_Password);


        // Header
        // TODO Inheritance
        //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
        //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
        TypeIdentifier identifier;
        identifier._d(EK_MINIMAL);

        eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
        size_t current_alignment {0};
        SerializedPayload_t payload(static_cast<uint32_t>(
            calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
        eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
        // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
        eprosima::fastcdr::Cdr ser(
            fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
            eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
        payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        ser << *type_object;
    #if FASTCDR_VERSION_MAJOR == 1
        payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    #else
        payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
    #endif // FASTCDR_VERSION_MAJOR == 1
        MD5 objectHash;
        objectHash.update((char*)payload.data, payload.length);
        objectHash.finalize();
        for(int i = 0; i < 14; ++i)
        {
            identifier.equivalence_hash()[i] = objectHash.digest[i];
        }

        TypeObjectFactory::get_instance()->add_type_object("Logon", &identifier, type_object);
        delete type_object;
        return TypeObjectFactory::get_instance()->get_type_object("Logon", false);
    }

    const TypeObject* GetCompleteLogonObject()
    {
        const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("Logon", true);
        if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
        {
            return c_type_object;
        }

        TypeObject *type_object = new TypeObject();
        type_object->_d(EK_COMPLETE);
        type_object->complete()._d(TK_STRUCTURE);

        type_object->complete().struct_type().struct_flags().IS_FINAL(false);
        type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
        type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
        type_object->complete().struct_type().struct_flags().IS_NESTED(false);
        type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

        MemberId memberId = 0;
        CompleteStructMember cst_header;
        cst_header.common().member_id(memberId++);
        cst_header.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_header.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_header.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_header.common().member_flags().IS_OPTIONAL(false);
        cst_header.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_header.common().member_flags().IS_KEY(false);
        cst_header.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_header.common().member_type_id(*DistributedATS::GetHeaderIdentifier(true));

        cst_header.detail().name("header");

        type_object->complete().struct_type().member_seq().emplace_back(cst_header);

        CompleteStructMember cst_EncryptMethod;
        cst_EncryptMethod.common().member_id(memberId++);
        cst_EncryptMethod.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_EncryptMethod.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_EncryptMethod.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_EncryptMethod.common().member_flags().IS_OPTIONAL(false);
        cst_EncryptMethod.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_EncryptMethod.common().member_flags().IS_KEY(false);
        cst_EncryptMethod.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_EncryptMethod.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_EncryptMethod.detail().name("EncryptMethod");

        type_object->complete().struct_type().member_seq().emplace_back(cst_EncryptMethod);

        CompleteStructMember cst_HeartBtInt;
        cst_HeartBtInt.common().member_id(memberId++);
        cst_HeartBtInt.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_HeartBtInt.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_HeartBtInt.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_HeartBtInt.common().member_flags().IS_OPTIONAL(false);
        cst_HeartBtInt.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_HeartBtInt.common().member_flags().IS_KEY(false);
        cst_HeartBtInt.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_HeartBtInt.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("int32_t", false));


        cst_HeartBtInt.detail().name("HeartBtInt");

        type_object->complete().struct_type().member_seq().emplace_back(cst_HeartBtInt);

        CompleteStructMember cst_RawData;
        cst_RawData.common().member_id(memberId++);
        cst_RawData.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_RawData.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_RawData.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_RawData.common().member_flags().IS_OPTIONAL(false);
        cst_RawData.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_RawData.common().member_flags().IS_KEY(false);
        cst_RawData.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_RawData.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_RawData.detail().name("RawData");

        type_object->complete().struct_type().member_seq().emplace_back(cst_RawData);

        CompleteStructMember cst_Username;
        cst_Username.common().member_id(memberId++);
        cst_Username.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Username.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Username.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Username.common().member_flags().IS_OPTIONAL(false);
        cst_Username.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Username.common().member_flags().IS_KEY(false);
        cst_Username.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Username.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_Username.detail().name("Username");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Username);

        CompleteStructMember cst_Password;
        cst_Password.common().member_id(memberId++);
        cst_Password.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
        cst_Password.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
        cst_Password.common().member_flags().IS_EXTERNAL(false); // Unsupported
        cst_Password.common().member_flags().IS_OPTIONAL(false);
        cst_Password.common().member_flags().IS_MUST_UNDERSTAND(false);
        cst_Password.common().member_flags().IS_KEY(false);
        cst_Password.common().member_flags().IS_DEFAULT(false); // Doesn't apply
        cst_Password.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

        cst_Password.detail().name("Password");

        type_object->complete().struct_type().member_seq().emplace_back(cst_Password);


        // Header
        type_object->complete().struct_type().header().detail().type_name("Logon");
        // TODO inheritance
        TypeIdentifier identifier;
        identifier._d(EK_COMPLETE);

        eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
        size_t current_alignment {0};
        SerializedPayload_t payload(static_cast<uint32_t>(
            calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
        eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
        // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
        eprosima::fastcdr::Cdr ser(
            fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
            eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
        payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

        ser << *type_object;
    #if FASTCDR_VERSION_MAJOR == 1
        payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
    #else
        payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
    #endif // FASTCDR_VERSION_MAJOR == 1
        MD5 objectHash;
        objectHash.update((char*)payload.data, payload.length);
        objectHash.finalize();
        for(int i = 0; i < 14; ++i)
        {
            identifier.equivalence_hash()[i] = objectHash.digest[i];
        }

        TypeObjectFactory::get_instance()->add_type_object("Logon", &identifier, type_object);
        delete type_object;
        return TypeObjectFactory::get_instance()->get_type_object("Logon", true);
    }

} // namespace DistributedATS_Logon